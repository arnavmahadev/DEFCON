# Personal working notes / task tracker — never commit
tasks.md

# Data (private tracking data must never be committed; large files stay local)
# Anchored to the repo root so it does NOT also swallow src/defcon/data/ (source).
/data/
*.csv
*.parquet
*.h5
*.hdf5
# ...but keep the small *synthetic* PFF-format test fixtures (our own data, not PFF's)
!tests/data/
!tests/data/**

# Model checkpoints & artifacts
*.pt
*.pth
*.pkl
*.joblib
checkpoints/
runs/
wandb/
mlruns/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.ipynb_checkpoints/

# Tooling / OS
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
.idea/
.vscode/

# CatBoost training logs
catboost_info/
